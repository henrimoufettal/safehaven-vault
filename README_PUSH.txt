# Push SafeHaven Vault to GitHub

Follow these steps in your terminal:

```bash
cd safehaven_vault_flutter_mvp

git init
git add .
git commit -m "Initial commit - SafeHaven Vault"

git remote add origin https://github.com/henrimoufettal/safehaven-vault.git
git push -u origin master
```

📌 Make sure you unzip the project and run these commands in the root folder.

After pushing, your repo will be available at:
🔗 https://github.com/henrimoufettal/safehaven-vault
